{
  "statements": [{
    "query": "MATCH (n:AWSPrincipal {guard0_org_id: $GUARD0_ORG_ID}) WHERE NOT (n)<-[:RESOURCE {guard0_org_id: $GUARD0_ORG_ID}]-(:AWSAccount {guard0_org_id: $GUARD0_ORG_ID}) AND n.lastupdated <> $UPDATE_TAG WITH n LIMIT $LIMIT_SIZE DETACH DELETE (n)",
    "iterative": true,
    "iterationsize": 100
  }],
  "name": "cleanup AWSPrincipal"
}
