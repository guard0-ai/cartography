{
  "name": "GitHubBranchProtectionRule RESOURCE edge migration",
  "statements": [
    {
      "__comment__": "DEPRECATED: temporary backward-compatibility migration. Earlier versions scoped GitHubBranchProtectionRule cleanup via HAS_RULE (per-repo); the schema now uses RESOURCE from GitHubOrganization as the sub_resource. Backfill the RESOURCE edge for every existing rule reachable from the currently-syncing GitHubOrganization via its repositories. Scoped by $owner_org_id so multi-org refreshes don't redo the full graph each time. Idempotent (MERGE). Remove in v1.0.0.",
      "query": "MATCH (o:GitHubOrganization {guard0_org_id: $GUARD0_ORG_ID, id: $owner_org_id})-[:RESOURCE {guard0_org_id: $GUARD0_ORG_ID}]->(r:GitHubRepository {guard0_org_id: $GUARD0_ORG_ID})-[:HAS_RULE {guard0_org_id: $GUARD0_ORG_ID}]->(rule:GitHubBranchProtectionRule {guard0_org_id: $GUARD0_ORG_ID}) MERGE (o)-[rel:RESOURCE {guard0_org_id: $GUARD0_ORG_ID}]->(rule) ON CREATE SET rel.lastupdated = rule.lastupdated SET rel.guard0_org_id = $GUARD0_ORG_ID",
      "iterative": false
    }
  ]
}
